/**
 * PudimMod - Commands~pudim.js
 *
 * Custom pudim-* network command handlers have been removed to avoid OOS in
 * multiplayer. All features now use only standard game commands (construct,
 * gather, garrison, walk, barter) which both modded and unmodded clients handle
 * identically.
 */
